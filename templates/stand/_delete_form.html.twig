<form method="post" action="{{ path('app_stand_delete', {'id': stand.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stand.id) }}">
    <button class="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700 transition duration-300 ease-in-out">Delete</button>
</form>
