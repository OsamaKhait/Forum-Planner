{% extends 'base.html.twig' %}

{% block title %}Create New Stand{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-gray-100">
        <div class="bg-white shadow-xl rounded-lg w-full max-w-lg p-8">
            <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">Create New Stand</h1>

            {{ include('stand/_form.html.twig') }}

            <div class="mt-6 text-center">
                <a href="{{ path('app_stand_index') }}" class="text-blue-500 hover:text-blue-700 font-medium transition duration-300 ease-in-out">
                    ← Back to List
                </a>
            </div>
        </div>
    </div>
{% endblock %}
