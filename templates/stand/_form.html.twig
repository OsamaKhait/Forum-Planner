{{ form_start(form, {'attr': {'class': 'space-y-4'}}) }}
    {{ form_widget(form) }}
<button class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition duration-300 ease-in-out">
    {{ button_label|default('Save') }}
</button>
{{ form_end(form) }}
