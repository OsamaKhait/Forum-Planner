{% extends 'base.html.twig' %}

{% block title %}Create a New Forum{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center bg-gray-100">
        <div class="bg-white shadow-xl rounded-lg w-full max-w-lg p-8">
            <h1 class="text-4xl font-extrabold text-center text-gray-900 mb-8">Create a New Forum</h1>

            <div class="mb-8">
                {{ include('forum/_form.html.twig', {'button_label': 'Create Forum'}) }}
            </div>

            <div class="text-center">
                <a href="{{ path('app_forum_index') }}" class="text-blue-500 hover:text-blue-700 font-medium transition duration-300 ease-in-out">
                    ← Back to Forum List
                </a>
            </div>
        </div>
    </div>
{% endblock %}
